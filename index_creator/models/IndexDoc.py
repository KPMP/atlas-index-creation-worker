class IndexDoc:
    def __init__(self, access, platform, experimental_strategy, data_category, workflow_type, data_format, data_type, file_id, file_name, file_size, package_id, dois, cases):
        self.access = access
        self.platform = platform
        self.experimental_strategy = experimental_strategy
        self.data_category = data_category
        self.workflow_type = workflow_type
        self.file_id = file_id
        self.file_name = file_name
        self.data_format = data_format
        self.file_size = file_size
        self.data_type = data_type
        self.package_id = package_id
        self.dois = dois
        self.cases = cases